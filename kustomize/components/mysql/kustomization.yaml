
apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

secretGenerator:
  - name: mysql-secret
    envs:
      - secrets

generatorOptions:
  disableNameSuffixHash: true


resources:
- deployment.yaml
- svc.yaml
